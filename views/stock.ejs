<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>📦 Stock Overview | NAFFAZ & CO</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f4f6f8;
      padding: 20px;
      font-family: 'Segoe UI', sans-serif;
    }
    .container {
      background-color: #fff;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
    }
    h1 {
      font-weight: 600;
      color: #333;
    }
    table {
      margin-top: 20px;
    }
    th {
      background-color: #0d6efd;
      color: white;
    }
    td {
      vertical-align: middle;
    }
    .back-btn {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">📦 Current Stock</h1>

    <table class="table table-bordered table-hover text-center">
      <thead>
        <tr>
          <th scope="col">Item Name</th>
          <th scope="col">Quantity in Stock</th>
        </tr>
      </thead>
      <tbody>
        <% if (stock.length === 0) { %>
          <tr>
            <td colspan="2" class="text-muted">No items in stock.</td>
          </tr>
        <% } else { %>
          <% stock.forEach(item => { %>
            <tr>
              <td><%= item.item_name %></td>
              <td><%= item.quantity %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>

    <a href="/" class="btn btn-outline-primary back-btn">← Back to Dashboard</a>
  </div>
</body>
</html>
