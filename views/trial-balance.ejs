<!DOCTYPE html>
<html>
<head>
  <title>Trial Balance</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light p-5">
<div class="container">
  <h2>📘 Trial Balance</h2>
  <table class="table table-bordered bg-white">
    <thead class="table-dark">
      <tr>
        <th>Account</th>
        <th>Debit (৳)</th>
        <th>Credit (৳)</th>
      </tr>
    </thead>
    <tbody>
      <tr><td>Cash</td><td><%= cash > 0 ? cash : '' %></td><td><%= cash < 0 ? -cash : '' %></td></tr>
      <tr><td>Debtors</td><td><%= debtors %></td><td></td></tr>
      <tr><td>Creditors</td><td></td><td><%= creditors %></td></tr>
    </tbody>
    <tfoot class="table-light">
      <tr>
        <th>Total</th>
        <th><%= (cash > 0 ? cash : 0) + debtors %></th>
        <th><%= (cash < 0 ? -cash : 0) + creditors %></th>
      </tr>
    </tfoot>
  </table>
  <a href="/" class="btn btn-secondary">⬅ Back</a>
</div>
</body>
</html>
